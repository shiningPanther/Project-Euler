#include <iostream>
#include <vector>
using namespace std;


long Problem15();
long reciprocalArray(int, int, vector<vector<long> >&);
long bottomUp(int, int);
long binomial(int);
long long factorial(int, int=0);

